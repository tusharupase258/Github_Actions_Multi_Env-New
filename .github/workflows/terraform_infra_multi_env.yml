name: Terraform Deployment

on:
  push:
    branches:
      - dev
      - preprod
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: "Select environment"
        required: true
        type: choice
        options:
          - dev
          - preprod
          - prod

jobs:
  terraform:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || github.ref_name }}
    env:
      ENVIRONMENT: ${{ github.event.inputs.environment || github.ref_name }}
      TF_DIR: 01.Modules/${{ github.event.inputs.environment || github.ref_name }}
      ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
      ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform -chdir=$TF_DIR init

      - name: Terraform Plan
        run: terraform -chdir=$TF_DIR plan

      - name: Terraform Apply (Auto)
        if: env.ENVIRONMENT == 'dev' || env.ENVIRONMENT == 'preprod'
        run: terraform -chdir=$TF_DIR apply -auto-approve

      - name: Terraform Apply (Manual)
        if: env.ENVIRONMENT == 'main' || env.ENVIRONMENT == 'prod'
        run: terraform -chdir=$TF_DIR apply -auto-approve
